name: heac-0.2
channels:
  - conda-forge
  - defaults
dependencies:
  # ===================================================================
  # 基础 Python 环境
  # ===================================================================
  - python=3.11
  
  # ===================================================================
  # 核心科学计算（优先使用 conda 安装，更稳定）
  # ===================================================================
  - numpy>=1.24.0
  - pandas>=2.0.0
  - scipy>=1.11.0
  
  # ===================================================================
  # 数据可视化
  # ===================================================================
  - matplotlib>=3.8.0
  - seaborn>=0.13.0
  - plotly>=5.18.0
  
  # ===================================================================
  # 机器学习框架（conda 安装更稳定）
  # ===================================================================
  - scikit-learn>=1.3.0
  - xgboost>=2.0.0
  - lightgbm>=4.0.0
  
  # ===================================================================
  # 工具库
  # ===================================================================
  - joblib>=1.3.0
  - pip
  
  # ===================================================================
  # 通过 pip 安装的专用包（conda 不可用或版本特定）
  # ===================================================================
  - pip:
    # 材料科学专用库
    - pymatgen>=2023.0.0
    - mp-api>=0.41.0
    - emmet-core>=0.70.0
    - matminer>=0.9.0
    
    # Web 应用
    - streamlit>=1.30.0
    
    # 机器学习扩展
    - catboost>=1.2.0
    - optuna>=3.5.0
    - shap>=0.42.0
    
    # 工具
    - python-dotenv>=1.0.0

